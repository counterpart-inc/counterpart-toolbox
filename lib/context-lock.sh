#!/usr/bin/env bash
# lib/context-lock.sh — loads context-lock functions for use by yourcounterpart CLI
# Real logic lives in scripts/context-lock.sh (self-contained, copy into any repo).

# shellcheck source=scripts/context-lock.sh
source "${TOOLBOX_DIR}/scripts/context-lock.sh"
