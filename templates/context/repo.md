# [REQUIRED] What Is This Repo?

One paragraph. What problem does this repo solve? Who are the users? What does it NOT do?

## [REQUIRED] How To Run

Step-by-step local setup:

```bash
# 1. Install dependencies
[command]

# 2. Configure environment
cp .env.example .env
# Edit .env with your local values

# 3. Start services
[command]

# 4. Run the app
[command]
```

**Prerequisites**: [e.g. Docker, Python 3.12, Node 18+]

## [REQUIRED] Tech Stack

- **Language**: [e.g. Python 3.12 / TypeScript 5.x]
- **Framework**: [e.g. Django 4.2 / React 18]
- **Database**: [e.g. PostgreSQL 15]
- **Cache/Queue**: [e.g. Redis + Celery]
- **Tests**: [e.g. pytest / Vitest]
- **Build**: [e.g. uv / npm / bun]

## [REQUIRED] Architecture

Key directories and what lives where:

```
[repo]/
├── [dir]/      # [What lives here]
├── [dir]/      # [What lives here]
└── [dir]/      # [What lives here]
```

Non-obvious structural decisions:
- [Decision]: [Brief rationale]

## [OPTIONAL] Key Decisions

Important architectural choices made and why:

- **[Decision]**: [Rationale. When was it made? Is it still the right call?]

## [OPTIONAL] Known Gotchas

Things that trip up new devs:

- **[Gotcha]**: [What happens and how to avoid/fix it]
